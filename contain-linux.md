# Step 1: Add `contain-linux.thm` to `/etc/hosts`

To make this write-up consistent, execute `echo <IPv4 address of CONTAIN Linux VM> contain-linux.thm >> /etc/hosts`.

## 1. Open the `/etc/hosts` file
You need root privileges to edit this file. You can use `nano` or `vim`:

```bash
sudo nano /etc/hosts
